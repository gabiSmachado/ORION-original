apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mcp-client.fullname" . }}
  labels:
    {{- include "mcp-client.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mcp-client.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mcp-client.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        {{- range $k, $v := .Values.podAnnotations }}
        {{ $k }}: {{ $v | quote }}
        {{- end }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: mcp-client
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # The client waits for stdin; keep container alive by tailing /dev/null if no TTY input.
          command: ["python", "main.py"]
          #command: ["tail", "-f", "/dev/null"]
          workingDir: /app
          ports:
            - name: http
              containerPort: 9100
              protocol: TCP
          env:
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            {{- if or .Values.openai.apiKey .Values.openai.existingSecret }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.openai.existingSecret }}{{ .Values.openai.existingSecret }}{{ else }}{{ include "mcp-client.fullname" . }}-openai{{ end }}
                  key: {{ .Values.openai.secretKey }}
            {{- end }}
            {{- if or .Values.anthropic.apiKey .Values.anthropic.existingSecret }}
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.anthropic.existingSecret }}{{ .Values.anthropic.existingSecret }}{{ else }}{{ include "mcp-client.fullname" . }}-anthropic{{ end }}
                  key: {{ .Values.anthropic.secretKey }}
            {{- end }}
            {{- if or .Values.gemini.apiKey .Values.gemini.existingSecret }}
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.gemini.existingSecret }}{{ .Values.gemini.existingSecret }}{{ else }}{{ include "mcp-client.fullname" . }}-gemini{{ end }}
                  key: {{ .Values.gemini.secretKey }}
            {{- end }}
            {{- if or .Values.qwen.apiKey .Values.qwen.existingSecret }}
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.qwen.existingSecret }}{{ .Values.qwen.existingSecret }}{{ else }}{{ include "mcp-client.fullname" . }}-qwen{{ end }}
                  key: {{ .Values.qwen.secretKey }}
            {{- end }}            
          volumeMounts:
            {{- if .Values.config.enabled }}
            - name: config
              mountPath: /app/config/config.yaml
              subPath: config.yaml
            {{- end }}
          {{- if .Values.probes.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.path }}
              port: 8080
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
            successThreshold: {{ .Values.probes.successThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.path }}
              port: 8080
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
            successThreshold: {{ .Values.probes.successThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.config.enabled }}
        - name: config
          configMap:
            name: {{ include "mcp-client.fullname" . }}-config
        {{- end }}
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml .Values.affinity | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
